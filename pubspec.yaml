name: seed
description: Seed-based value and identity abstraction for Dart.
version: 0.1.0
repository: https://github.com/jack8096/seed
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.11.5

# Add regular dependencies here.
dependencies:
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
